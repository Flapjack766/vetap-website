# دليل البدء السريع - VETAP

## 📋 المتطلبات

- Node.js 18 أو أحدث
- npm أو yarn أو pnpm

## 🚀 التثبيت والتشغيل

### 1. تثبيت الحزم

```bash
npm install
```

### 2. إعداد ملف البيئة

انسخ ملف `.env.example` إلى `.env.local`:

```bash
cp .env.example .env.local
```

### 3. تعديل متغيرات البيئة

افتح `.env.local` وأضف المفاتيح الخاصة بك:

```env
RESEND_API_KEY=re_xxxxxxxxxxxxx
COMPANY_EMAIL=support@vetaps.com
COMPANY_NAME=VETAP
FROM_EMAIL="VETAP <no-reply@vetaps.com>"
SITE_URL=https://vetaps.com
```

**مهم**: احصل على مفتاح API من [Resend](https://resend.com) لتفعيل نموذج التواصل.

### 4. تشغيل المشروع

```bash
npm run dev
```

افتح [http://localhost:3000/en](http://localhost:3000/en) في المتصفح.

## 🌍 اللغات المتاحة

- **الإنجليزية**: http://localhost:3000/en
- **العربية**: http://localhost:3000/ar

## 📄 الصفحات

- **الرئيسية**: `/[locale]`
- **الخدمات**: `/[locale]/services`
- **معرض الأعمال**: `/[locale]/portfolio`
- **من نحن**: `/[locale]/about`
- **تواصل معنا**: `/[locale]/contact`

## 🛠️ البناء للإنتاج

```bash
npm run build
npm run start
```

## ✅ فحص الأخطاء

```bash
# TypeScript type checking
npm run typecheck

# ESLint
npm run lint
```

## 📦 الميزات الرئيسية

✅ **واجهة تفاعلية** - HeroShowcase مع أسئلة ذكية
✅ **نموذج اتصال متقدم** - مع نظام تذاكر ورسائل بريدية
✅ **دعم كامل للغة العربية** - مع RTL
✅ **SEO محسّن** - مع sitemap و robots.txt و JSON-LD
✅ **أمان عالي** - CSP headers و rate limiting
✅ **أداء فائق** - SSG و image optimization

## 📧 اختبار نموذج التواصل

1. انتقل إلى صفحة التواصل: `/en/contact` أو `/ar/contact`
2. املأ النموذج
3. ستتلقى رسالة تأكيد في بريدك الإلكتروني
4. سيتلقى البريد المؤسسي إشعاراً بالطلب

## 🎨 التخصيص

### تغيير الألوان

عدّل ملف `styles/themes.css` لتخصيص الألوان:

```css
:root {
  --vetap-black: #0a0a0a;
  --vetap-white: #ffffff;
  /* ... */
}
```

### إضافة صور

ضع الصور في:
- `public/images/` - للصور العامة
- `public/icons/` - للأيقونات والشعار

### تعديل الترجمات

عدّل الملفات:
- `content/en.json` - الترجمة الإنجليزية
- `content/ar.json` - الترجمة العربية

## 🔧 حل المشاكل الشائعة

### خطأ: "Cannot find module 'next-intl'"

```bash
npm install
```

### نموذج التواصل لا يعمل

تأكد من:
1. إضافة `RESEND_API_KEY` في `.env.local`
2. تفعيل المفتاح في حساب Resend
3. التحقق من صحة البريد الإلكتروني

### الموقع لا يظهر بشكل صحيح

```bash
# امسح ذاكرة التخزين المؤقت
rm -rf .next
npm run dev
```

## 📚 الموارد

- [Next.js Documentation](https://nextjs.org/docs)
- [next-intl Documentation](https://next-intl-docs.vercel.app/)
- [Resend Documentation](https://resend.com/docs)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)

## 🆘 الدعم

لأي استفسارات أو مشاكل، يرجى زيارة صفحة التواصل أو إرسال بريد إلكتروني إلى support@vetaps.com

---

صُنع بـ ❤️ بواسطة VETAP

